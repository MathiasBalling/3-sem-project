#include "consts.h"
#include "goertzel.h"

int main() {
  int sampleRate = 48000;
  int numSamples = 500;
  float data[numSamples] for (int i = 0; i < 100000; i++)

      findDTMF(500, 48000, nullptr);
  return 0;
}
