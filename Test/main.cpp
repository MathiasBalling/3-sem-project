#include "PAsound.h"

int main(int argc, char *argv[]) {
  int duration = 1000;
  PAsound sound;
  sound.playtest();
  // sound.beep(697, 1209, duration); // 1
  // b.beep(697, 1336, duration); //2
  // b.beep(697, 1477, duration); //3
  // b.beep(697, 1633, duration); //A
  // b.beep(770, 1209, duration); //4
  // b.beep(770, 1336, duration); //5
  // b.beep(770, 1477, duration); //6
  // b.beep(770, 1633, duration); //B
  // b.beep(852, 1209, duration); //7
  // b.beep(852, 1336, duration); //8
  // b.beep(852, 1477, duration); //9
  // b.beep(852, 1633, duration); //C
  // b.beep(941, 1209, duration); //*
  // b.beep(941, 1336, duration); //0
  // b.beep(941, 1477, duration); //#
  // b.beep(941, 1633, duration); //D

  // b.wait();
  return 0;
}